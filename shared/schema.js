// MongoDB schema types for Task Manager
// User: { _id?, email, password, createdAt? }
// Task: { _id?, userId, title, description, completed, createdAt?, updatedAt? }
// InsertUser: { email, password }
// InsertTask: { userId, title, description, completed? }
